[project]
name = "pillows-finetune"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "sentence_transformers",
  "typer",
  "torch",
  "datasets>=3.6.0",
  "tqdm>=4.67.1",
  "accelerate>=1.8.1",
]

